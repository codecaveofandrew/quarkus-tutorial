apiVersion: v1
kind: Service
metadata:
  name: code-with-quarkus
spec:
  selector:
    app: code-with-quarkus
  ports:
    - protocol: TCP
      port: 80  # 公开的端口，用户通过这个端口访问你的服务
      targetPort: 8080  # 应用程序在容器内监听的端口
  type: LoadBalancer